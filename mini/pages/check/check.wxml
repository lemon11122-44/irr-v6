<navigation-bar title="各平台扣费自查" color="black" background="#fff" />
<scroll-view class="pg" scroll-y>
<view class="hd"><text class="ht">各平台扣费明细</text><text class="hs">以下为常见网贷平台信息，仅供参考</text></view>
<view class="ls">
<view class="it" wx:for="{{list}}" wx:key="id">
<view class="il"><image class="ii" src="{{item.ic}}" wx:if="{{item.ic}}" /><view class="if" wx:else><text>{{item.nm.charAt(0)}}</text></view></view>
<view class="ir"><text class="in">{{item.nm}}</text><text class="ik">{{item.tp}}</text><text class="id" wx:if="{{item.ds}}">{{item.ds}}</text></view>
</view>
</view>
<view class="tp">以上信息仅供参考，以各平台实际为准</view>
</scroll-view>