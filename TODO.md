# IRR v6 项目 — 功能迭代任务清单

## 项目路径
- 后端: `C:\Users\Administrator\irr-v6\backend\`
- 小程序: `C:\Users\Administrator\irr-v6\mini\`
- 后端端口: 9900
- 运行命令: `cd C:\Users\Administrator\irr-v6\backend && python run6.py`

## 管理员账号
- 后台: http://127.0.0.1:9900/adm/login
- 账号: root / 密码: root888

---

## 一、小程序功能迭代（优先级从高到低）

### 任务1：首页 IRR 计算器——结果展示美化
**文件**: `mini/pages/home/home.wxml` + `home.wxss`

**现状**: 结果用 `{{Object.entries(result)}}` 遍历显示所有键值对，包含英文变量名

**要求**:
1. 为每种计算模式(9种)定义中文结果字段映射，例如:
   - `monthlyPmt` → `每月还款`
   - `totalInterest` → `总利息`
   - `totalRepay` → `本息合计`
   - `annualIRR` → `实际年化IRR`
   - `excessOver24` → `超出24%利息`
   - 等
2. 结果卡片改用美观的分组展示:
   - 分组1: 每月还款信息（蓝色高亮）
   - 分组2: 利息汇总（橙色）
   - 分组3: 法率判断（红色/绿色）
3. 各字段数值自动加单位后缀（元、%、次）

### 任务2：首页——轮播支持后台动态配置
**文件**: `mini/pages/home/home.js`

**现状**: 轮播文字写死「IRR真实年化怎么算？」

**要求**:
1. 在 `onLoad` 中调用 `http://192.168.100.20:9900/n6/cg` 获取配置
2. 根据 `bn_t`(标题) 和 `bn_s`(副标题) 更新轮播文字
3. 若无返回数据则显示默认文字

### 任务3：首页——客服浮窗读取后台数据
**文件**: `mini/pages/home/home.js`

**现状**: 客服点击复制写死的 `kf@irr.com`

**要求**:
1. 在 `onLoad` 中调用 `http://192.168.100.20:9900/n6/sv` 获取客服列表
2. 取第一个在线(`ol=true`)客服的 `ph`(电话)作为联系方式
3. 点击复制该号码，提示「已复制客服联系方式」

### 任务4：扣费自查页面——完善图文展示
**文件**: `mini/pages/check/check.wxml` + `check.wxss`

**现状**: 简单列表

**要求**:
1. 若列表为空，显示「暂无平台数据」空状态占位
2. 添加加载状态（loading 动画）
3. 请求失败时显示「加载失败，点击重试」按钮
4. 列表样式优化：添加分隔线、平台分类用彩色标签

### 任务5：新增项目配置文件
**文件**: `mini/project.config.json`（已存在，检查并补充）

**要求**:
1. 确保 `urlCheck: false`（已配置则跳过）
2. 确保 `appid` 填写正确（wx69a98d2a2e0f0b96）

---

## 二、后台功能补全

### 任务6：后台——平台管理增加图片上传实时预览
**文件**: `backend/t6/p6.html`

**现状**: 添加/编辑平台弹窗的图片上传组件已实现基本功能

**要求**:
1. 上传后图片立即在弹窗内显示缩略图（已有此功能，验证可用性）
2. 添加删除已上传图片按钮
3. 编辑弹窗中保留已有图片的URL显示

### 任务7：后台——客服管理完善
**文件**: `backend/t6/p6.html` + `backend/r6/api.py`

**现状**: 客服CRUD已实现，头像上传可用

**要求**:
1. 客服页面独立为一个导航菜单入口（`/adm/svc`）—— 目前已在 `web.py` 注册但导航栏没有入口
2. 在 admin.html 顶部导航增加「客服管理」菜单（目前4个菜单，改为5个）
3. 确保所有编辑弹窗的表单控件无 readonly/disabled

### 任务8：后台——系统配置增加客服微信号配置
**文件**: `backend/t6/p6.html` + `backend/main6.py`

**要求**:
1. 系统配置页面增加字段：`kf_phone`（客服电话/微信号）
2. 保存后小程序端读取该配置
3. 系统配置页面UI对齐其他页面风格

### 任务9：后台——概览页统计展示
**文件**: `backend/t6/p6.html`

**现状**: 概览页显示4格统计数字

**要求**:
1. 每个统计卡片添加对应管理入口链接
2. 增加「平台」「文章」「客服」「配置」图标

---

## 三、提交流程

每完成一个任务，请在群里说一声，我来验证。
验证通过后再进行下一个任务。
若遇到技术问题随时问我。

---

## 四、代码规范

1. 新增/修改前先读一遍相关文件完整内容
2. JS/Python 用 UTF-8 编码
3. 小程序 wxss 用 rpx 单位
4. 所有 API URL 使用 `http://192.168.100.20:9900`（本地局域网）
5. 后端运行中，可直接修改代码后自动生效（uvicorn reload未启用，改完需重启）
